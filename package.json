{
  "name": "express-distributors",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"cd backend && PATH=/opt/homebrew/bin:$PATH npm run dev\" \"cd frontend && PATH=/opt/homebrew/bin:$PATH npm run dev\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "start": "npm run start --workspace=backend",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "5.6.3"
  },
  "overrides": {
    "typescript": "5.6.3"
  }
}
